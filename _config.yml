title: calebbrown.id.au
author: Caleb Brown
theme: jekyll-theme-hacker
defaults:
 - scope:
    path: "static"
   values:
     images: true
 - scope:
    path: "images"
   values:
     images: true

plugins:
 - jekyll-redirect-from

